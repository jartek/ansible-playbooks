---
  - name: install supervisor
    apt: "pkg={{ item }} state=installed"
    with_items:
      - supervisor

  - name: ensure supervisor service is up and running
    service: name=supervisor enabled=yes state=started

  - name: Copy supervisor conf
    copy: src=templates/supervisor.conf dest=/etc/supervisor/conf.d/main.conf
